@model IEnumerable<BTL.Models.Lop>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaLop)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GiaoVien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaMonHoc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaLop)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GiaoVien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaMonHoc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TenLop }) |
            @Html.ActionLink("Details", "Details", new { id=item.TenLop }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TenLop })
        </td>
    </tr>
}

</table>
